STALE OUTPUT REMOVED FROM OPERATIONAL USE

This file previously contained an old Vercel CLI environment listing whose displayed project name did not match the canonical linked project `right-at-home-bnb`.

Do not use this artifact as deployment evidence.

Generate fresh, value-redacted evidence from the linked project after the HAMMER command route is restored:

  vercel project inspect
  vercel env ls

Required Firebase authority: rightathome-prod
Secret values must never be written to this file.
